import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    buttons: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center'
    },
    video: {
        width: 320,
        height: 200
    }
})
