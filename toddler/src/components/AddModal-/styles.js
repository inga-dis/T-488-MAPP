import { StyleSheet } from "react-native";

export default StyleSheet.create({
    icon: {
        fontSize: 60, 
        marginTop: 20, 
        marginBottom: 20
    },
    input: {
        backgroundColor: 'ghostwhite',
        marginBottom: 8,
        padding: 8,
        height: 40,
      }
});