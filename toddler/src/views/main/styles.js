import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    text: {
        position: 'absolute',
        top: 15,
        right: 15,
        fontSize: 16,
        backgroundColor: "pink"
    }
});
